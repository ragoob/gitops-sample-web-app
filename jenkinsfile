pipeline {
  agent any
  environment {
    WORKSPACE = pwd()
    GOCACHE="/root/.cache/go-build"


  }

  stages {
      stage('unit-test') {
      agent { docker { image 'golang' } }
      steps {
        echo 'Unit test excuting'
          sh 'make unit-tests'
        
      }
      }
  }
}
