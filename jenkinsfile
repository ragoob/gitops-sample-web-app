pipeline{
  agent any
  environment {
    WORKSPACE= pwd()
  }

  stages {
      stage("unit-test") {
            agent { docker { image 'golang' } }
            steps {
                echo 'Unit test excuting'
                sh 'make unit-tests'
            }
        }
  }
}